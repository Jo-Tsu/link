# Security Policy

## Supported Version

只有最新的开发预览版本接受安全修复。当前 `0.2.x` 仅用于受信任电脑上的本机测试，不支持直接部署到公网。

## Reporting a Vulnerability

请使用代码托管平台的私密漏洞报告功能，不要在公开 Issue 中提交令牌、数据库内容、Codex 对话、本机路径或可直接利用的漏洞细节。

报告应包含受影响版本、复现条件、影响范围和最小复现步骤。维护者确认后会协调修复和披露时间。

## Current Security Boundary

- Web、PostgreSQL 和 Crawler 默认只绑定 `127.0.0.1`。
- LinkAgent 只主动连接平台，不开放本机入站端口。
- Agent 设备接口使用设备令牌；平台管理接口尚未提供公网用户认证。
- 当前版本没有多租户隔离、RLS、邮箱登录、配额和平台运营权限。
- 不要把数据库备份、`.env`、DMG、本地队列和 `~/.codex` 内容提交到仓库。

云端部署、多租户或公开网络使用不在当前支持范围内。
