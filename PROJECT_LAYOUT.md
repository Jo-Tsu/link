# Smallink 项目目录说明

当前目录是 Smallink 的唯一开发主目录。初始化或恢复 Git 元数据后，也应以此目录作为仓库根目录。

## 当前主工程

| 目录 | 内容 |
|---|---|
| `smallink/` | Python 后端、智能体、连接器、记忆与任务运行的唯一正式实现 |
| `link/` | 旧版 Python 导入兼容转发层，不承载业务实现，新功能禁止写入 |
| `surfaces/gui/` | Web 与 macOS 桌面客户端 |
| `stt/` | 本地语音输入模块 |
| `tests/` | 后端测试 |
| `docs/` | 当前 PRD、技术方案和产品文档 |
| `design/brand/` | Smallink 字标源文件、独立图形与品牌使用规则 |
| `packaging/` | macOS 与 Windows 打包脚本和产物 |
| `.venv/` | 当前项目 Python 环境 |

## 参考与历史资料

| 目录 | 内容 | 使用边界 |
|---|---|---|
| `references/mem0-main/` | mem0 参考源码 | 只用于设计参考，不作为 Link 运行依赖 |
| `archive/legacy-memory-prototype/` | 早期个人知识与记忆原型 | 只读历史资料，不作为当前产品事实源 |

## 不放入源码目录的数据

以下内容属于运行数据或用户数据，不能与源码、参考项目和构建产物混在一起：

- `~/.config/link/`：数据库、设置、密钥、日志和模型等本机状态。
- `~/Smallink/`：新建用户会话产生的默认工作目录和产物；已有偏好中的旧路径继续有效。
- 各项目自身的 `.link/`：该工作区的 Link 配置。

这些目录由 Link 在运行时管理。迁移或备份时应单独处理，不能提交到源码仓库。

## 命名空间规则

- 新增后端功能、连接器、工具、智能体、任务与记忆代码只能进入 `smallink/`。
- 正式 Python 导入统一使用 `smallink.*`。
- `link/`、`link-server`、`X-Link-Token` 和既有状态目录仅用于兼容历史安装；兼容层不能复制或分叉业务逻辑。
