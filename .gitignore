.venv/
.DS_Store
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
output/
.coverage

# --- Build products (do NOT commit; regenerated by packaging/build_dmg.sh) ---
# PyInstaller freeze of the server sidecar + its staged copy inside the Tauri app.
packaging/dist/
packaging/build/
surfaces/gui/src-tauri/binaries/sidecar/
# Tauri/Rust compile output (includes the final .app and .dmg) and Vite frontend build.
surfaces/gui/src-tauri/target/
surfaces/gui/dist/
# NOTE: surfaces/gui/src-tauri/icons/ and packaging/dmg-background.tiff ARE committed —
# they are build inputs the desktop bundle requires, not generated products.
